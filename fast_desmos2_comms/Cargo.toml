[package]
name = "fast_desmos2_comms"
version = "0.1.0"
edition = "2021"

[features]
default = ["client", "server"]
client = []
server = []

[dependencies]
bincode = "1.3.3"
glam = "0.29.0"
take_mut = "0.2.2"
uiua = { version = "0.14.1", default-features = false }


