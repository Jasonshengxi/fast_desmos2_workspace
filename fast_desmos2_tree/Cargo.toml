[package]
name = "fast_desmos2_tree"
version = "0.1.0"
edition = "2021"

[features]

[dependencies]
glam = "0.29.2"
take_mut = "0.2.2"
thiserror = "2.0.8"

fast_desmos2_utils = { path = "../fast_desmos2_utils" }
